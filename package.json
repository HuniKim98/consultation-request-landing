{
  "name": "consultation-landing-page",
  "version": "1.0.0",
  "type": "module",
  "description": "SK Escape Consultation Request Landing Page",
  "main": "index.html",
  "scripts": {
    "dev": "python3 -m http.server 5173",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r api dist/api 2>/dev/null || true",
    "preview": "python3 -m http.server 5173"
  },
  "dependencies": {},
  "devDependencies": {}
}
